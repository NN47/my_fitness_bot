# 🚀 Порядок действий после рефакторинга

## 📋 Что у вас есть сейчас

### ✅ Новая структура (готова к использованию)
- **`main.py`** - новая точка входа с модульной архитектурой
- **50+ модулей** с FSM, репозиториями и сервисами
- **Полная функциональность** перенесена

### 📦 Старая структура (сохранена)
- **`bot.py`** - старый файл (7734 строки) продолжает работать
- Можно использовать параллельно для сравнения

---

## 🎯 Варианты действий

### Вариант 1: Тестирование новой структуры (рекомендуется)

#### Шаг 1: Проверка окружения
```bash
# Убедитесь, что все зависимости установлены
pip install -r requirements.txt  # если есть requirements.txt
# или установите вручную:
pip install aiogram python-dotenv sqlalchemy matplotlib google-genai requests
```

#### Шаг 2: Проверка переменных окружения
Создайте/проверьте файл `.env`:
```env
API_TOKEN=your_telegram_bot_token
DATABASE_URL=sqlite:///fitness_bot.db
GEMINI_API_KEY=your_gemini_api_key
NUTRITION_API_KEY=your_calorieninjas_api_key
```

#### Шаг 3: Запуск нового бота
```bash
python main.py
```

#### Шаг 4: Тестирование функционала
Протестируйте основные функции:
- ✅ Команда `/start`
- ✅ Добавление тренировки
- ✅ Добавление КБЖУ
- ✅ Добавление веса
- ✅ Добавление добавки
- ✅ Добавление воды
- ✅ Календари
- ✅ Тест КБЖУ

#### Шаг 5: Сравнение со старым ботом
Если нужно, запустите старый бот для сравнения:
```bash
python bot.py  # если он запускается отдельно
```

---

### Вариант 2: Постепенная миграция

#### Шаг 1: Запуск нового бота параллельно
- Запустите `main.py` на тестовом окружении
- Проверьте, что всё работает

#### Шаг 2: Переключение на новую версию
- Остановите старый бот
- Запустите новый `main.py`
- Мониторьте логи на наличие ошибок

#### Шаг 3: Мониторинг
- Следите за логами в `logs/bot.log`
- Проверяйте работу всех функций
- Собирайте обратную связь от пользователей

---

### Вариант 3: Дальнейшая разработка

#### Шаг 1: Добавление новых функций
Теперь легко добавлять новые функции:

**Пример: Добавление новой функции**
1. Создайте обработчик в `handlers/`
2. Добавьте состояние в `states/user_states.py` (если нужно FSM)
3. Создайте репозиторий в `database/repositories/` (если нужна БД)
4. Зарегистрируйте в `main.py`

#### Шаг 2: Улучшение существующего кода
- Оптимизация запросов к БД
- Добавление кэширования
- Улучшение обработки ошибок

#### Шаг 3: Тестирование
- Написание unit-тестов для репозиториев
- Написание unit-тестов для сервисов
- Интеграционные тесты для handlers

---

## 🔧 Настройка и конфигурация

### 1. База данных
Новая структура использует ту же БД, что и старый бот:
- Файл: `fitness_bot.db` (SQLite)
- Миграции выполняются автоматически при первом запуске

### 2. Логирование
Логи теперь пишутся в:
- `logs/bot.log` - файл с логами
- Консоль - для отладки

Настройка уровней логирования в `utils/logging_config.py`

### 3. Конфигурация
Все настройки в `config.py`:
- API ключи
- Настройки БД
- Параметры серверов

---

## 📝 Что делать с bot.py

### Вариант A: Оставить как есть (рекомендуется)
- Старый `bot.py` остаётся как резервная копия
- Можно использовать для справки
- Не мешает новой структуре

### Вариант B: Архивировать
```bash
# Создайте архив
mv bot.py bot_old_backup.py
# или
mkdir archive
mv bot.py archive/bot_original.py
```

### Вариант C: Удалить (только после полного тестирования!)
```bash
# ⚠️ ВНИМАНИЕ: Удаляйте только после полного тестирования!
# rm bot.py
```

---

## 🐛 Решение проблем

### Проблема: Бот не запускается

**Проверьте:**
1. Установлены ли все зависимости
2. Правильно ли настроен `.env`
3. Есть ли права на запись в директорию (для БД и логов)

**Логи:**
```bash
# Проверьте логи
tail -f logs/bot.log
```

### Проблема: Ошибки БД

**Решение:**
- Проверьте, что файл БД существует
- Проверьте права доступа
- При необходимости пересоздайте БД (удалите `fitness_bot.db`)

### Проблема: Функция не работает

**Проверьте:**
1. Зарегистрирован ли обработчик в `main.py`
2. Правильно ли настроены состояния FSM
3. Проверьте логи на наличие ошибок

---

## 📚 Полезные файлы

### Документация
- **`REFACTORING_SUMMARY.md`** - подробный отчёт о рефакторинге
- **`REFACTORING_PROGRESS.md`** - прогресс рефакторинга
- **`README_REFACTORING.md`** - краткое описание структуры

### Код
- **`main.py`** - точка входа (запускайте это!)
- **`handlers/`** - все обработчики
- **`database/repositories/`** - работа с БД
- **`services/`** - внешние API
- **`utils/`** - утилиты

---

## ✅ Чек-лист перед запуском

- [ ] Установлены все зависимости
- [ ] Настроен файл `.env` с API ключами
- [ ] Проверена структура проекта
- [ ] Есть доступ к базе данных
- [ ] Есть права на запись логов
- [ ] Протестированы основные функции
- [ ] Проверены логи на ошибки

---

## 🎯 Рекомендуемый порядок действий

### 1. Сразу (5 минут)
```bash
# Проверьте окружение
python --version  # должен быть Python 3.8+
pip list | grep aiogram  # проверьте установку

# Проверьте .env
cat .env  # или откройте в редакторе
```

### 2. Тестирование (30 минут)
```bash
# Запустите новый бот
python main.py

# Протестируйте в Telegram:
# - /start
# - Добавление тренировки
# - Добавление КБЖУ
# - Просмотр календаря
```

### 3. Мониторинг (первые дни)
- Следите за логами
- Проверяйте работу всех функций
- Собирайте обратную связь

### 4. Дальнейшая разработка (по необходимости)
- Добавляйте новые функции
- Улучшайте существующий код
- Пишите тесты

---

## 💡 Советы

1. **Не удаляйте bot.py сразу** - оставьте как резервную копию
2. **Тестируйте постепенно** - не переключайте всех пользователей сразу
3. **Мониторьте логи** - они помогут найти проблемы
4. **Используйте git** - делайте коммиты перед большими изменениями
5. **Читайте документацию** - в модулях есть docstrings

---

## 🆘 Нужна помощь?

Если что-то не работает:
1. Проверьте логи в `logs/bot.log`
2. Проверьте конфигурацию в `.env`
3. Убедитесь, что все модули импортированы в `main.py`
4. Проверьте, что обработчики зарегистрированы

---

**Удачи с новым ботом! 🚀**
